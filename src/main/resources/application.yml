server:
  port: 9093

spring:
  application:
    name: FOOD-CATALOGUE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  profiles:
    active: dev